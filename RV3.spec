# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['/Users/vincenzobarone/centrifugal/RV3.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='RV3',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='RV3',
)
app = BUNDLE(
    coll,
    name='RV3.app',
    icon='/Users/vincenzobarone/centrifugal/assets/icons/rv3_app_icon.icns',
    bundle_identifier=None,
)
